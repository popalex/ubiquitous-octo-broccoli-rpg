[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests evals
markers =
    eval: real-model evaluation case; excluded from the default/CI run. Run with `pytest -m eval`.
addopts = -m "not eval"
